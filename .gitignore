# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/

# Virtual envs
.venv/
venv/

# Test / cache
.pytest_cache/
.ruff_cache/
.mypy_cache/

# HuggingFace model cache (can be several GB)
.cache/
*.safetensors
*.bin
*.ckpt

# brainimg runtime artifacts — all generated outputs live under outputs/
outputs/
# Stray root-level artifacts (in case something writes to CWD instead of outputs/)
*.brainimg
out/
recon.jpg
recon.png
recon_*.jpg
recon_*.png
# Sweep / run logs
phase2_sweep.log
*.sweep.log
# Generated side-by-side comparisons and per-backend reconstruction outputs.
# These are produced by decoder.py / scripts and are not tracked.
comparison.jpg
*_comparison.jpg
*_grid.jpg
!docs/grids/*.jpg
*_recon.png
# Per-backend reconstruction PNGs (e.g. mandril_sd15.png, peppers_zimage.png).
# These are large decoder outputs, not source — do not track.
mandril_*.png
peppers_*.png
cameraman_*.png
airplane_*.png
test512_recon.png

# uv lockfile (degenerate — real deps live in requirements.txt)
uv.lock

# Internal planning / publish-prep docs, not part of the public repo
docs/planning/
PLAN.md
TODO-PUBLISH.md

# OS
.DS_Store
# macOS AppleDouble resource-fork junk
._*
.___*
.___*